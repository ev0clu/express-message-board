<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
        <link rel="stylesheet" href="/stylesheets/style.css" />
    </head>
    <body>
        <main>
            <h1><%= title %></h1>
            <a href="/new" class="btn-container">
                <button type="click">New Message</button>
            </a>
            <div class="board-wrapper">
                <% messages.map(message => { %>
                <div class="message-wrapper">
                    <div class="name-date-wrapper">
                        <p class="name"><%=message.user%></p>
                        <p class="date"><%=message.createdAt%></p>
                    </div>
                    <div class="text-wrapper">
                        <p class="text"><%=message.text%></p>
                    </div>
                </div>
                <% }) %>
            </div>
        </main>
    </body>
</html>
